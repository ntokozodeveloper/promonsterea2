version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build:amplify
  artifacts:
    baseDirectory: web-based-ea-frontend/build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
